/**
 * WebStripePaymentForm.native.tsx
 *
 * Native platform stub — web Stripe Elements are not used on iOS/Android.
 * The native PaymentSheet flow (initPaymentSheet / presentPaymentSheet) handles
 * payments there instead.
 */

export function WebStripePaymentForm() {
  return null;
}
