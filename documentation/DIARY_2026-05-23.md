## Date
- 2026-05-23 (Saturday)

## Session Metadata Header
- Chat Name: zarkili
- Session ID: fix/new-debt-r-seed-scripts
- Project/Workspace: Zarkili
- Start Time: morning
- End Time: —
- Scope: NEW-DEBT-R — fix seed scripts to write canonical Firestore paths so discovery feed shows service cards

## Main Focus
- Fix all seed scripts so `getServiceCards()` collectionGroup query returns results in dev. The scripts wrote to `services/{id}` but the query reads from `service_types` (nested under `brands/{tenantId}/locations/{locationId}/`).

## Wins (What Moved Forward)
1. `seed-demo-services.mjs` and `seed-qa-firestore.mjs` now write to the canonical path `brands/{tenantId}/locations/{locationId}/service_types/{serviceId}`.
2. QA seed gained 13 missing denorm fields required by `getServiceCards()` — previously would have written queryable docs but they'd all render as blank cards.
3. CLEAR_SEED block in demo seed updated to delete both old stale `services/` path and new canonical path; idempotent re-seeding now works cleanly.
4. Warning comment added to top of both modified scripts — future devs see the constraint before touching paths.
5. 109 docs landed in `zarkili-dev-a1b1c`; quality gate clean (0 TS errors, 3685/3685 tests, 60 lint problems).

## Decisions Made
1. Decision: Leave `seed-discovery-featured-salons.mjs` unchanged.
- Why: It targets `discoveryFeaturedSalons` (a separate featured carousel collection, not `service_types`). It has no lat/lng data for geohash computation. Its 3 tenants are already fully covered by `seed-demo-services.mjs`.
- Impact: No service_type data gap — demo seed covers luna, glow, polish-room at the canonical path.

2. Decision: Remove `ALL_SERVICE_IDS` constant after CLEAR_SEED refactor.
- Why: Became unused when CLEAR_SEED was restructured to iterate `DEMO_LOCATIONS` (needed tenantId/locationId context for canonical path deletion). Lint caught it at 61 problems (+1 from baseline).
- Impact: Lint back to 60; no functional change.

## What I Actually Worked On
1. Task: NEW-DEBT-R implementation planning.
- Action taken: Read `getServiceCards()`, all 3 seed scripts, and the debt register entry. Compiled a 8-point plan covering paths, denorm fields, geohash, idempotency, CLEAR_SEED, and verification.
- Result: Plan approved by user before any code written.

2. Task: Path fixes in demo seed.
- Action taken: Changed 4 write paths (`service`, `variants`, `addons`, `variantCount update`) from `services/${id}` to `brands/${tenantId}/locations/${locationId}/service_types/${id}`. Restructured CLEAR_SEED to iterate `DEMO_LOCATIONS` and clean both old and new paths.
- Result: Dry run confirmed correct paths before real write.

3. Task: Path fixes + denorm fields in QA seed.
- Action taken: Changed 4 write paths (service, variants, addons, photos). Added 13 missing denorm fields to the service document: `geohash` (reused per-location variable), `locationLat`, `locationLng`, `locationDisplayName`, `locationCity`, `locationAverageRating`, `locationReviewCount`, `categoryName` (`svc.category`), `variantCount`, `priceFrom`, `durationFrom`, `primaryPhotoUrl`, `primaryPhotoSource`, `isBookableOnline`.
- Result: QA service docs now fully readable by `getServiceCards()`.

4. Task: Real seed run.
- Action taken: `npm run seed:demo:dev` against `zarkili-dev-a1b1c`.
- Result: 109 ops, 0 errors. 12 service_type docs, 25 variants, 13 addons, 9 staff, 9 schedules, 15 reviews.

## Artifacts Created/Updated
1. File: `scripts/seed-demo-services.mjs`
- Purpose: Path fix (`services/` → `brands/…/service_types/`), CLEAR_SEED update, warning comment, unused const removed.

2. File: `scripts/seed-qa-firestore.mjs`
- Purpose: Path fix + 13 missing denorm fields on all service documents, warning comment.

3. File: `documentation/new-platform/DEBT_REGISTER.md`
- Purpose: Closed NEW-DEBT-R with close notes.

## Risks/Blockers
1. Risk: Old `services/{id}` documents still exist in dev Firestore from previous seed runs.
- Current status: Low risk — they're invisible to `collectionGroup("service_types")` and will never be queried. CLEAR_SEED now handles cleanup of both paths when run.
- Mitigation/next action: Run `CLEAR_SEED=true npm run seed:demo:dev` once if stale docs are a concern.

2. Risk: `seed-discovery-featured-salons.mjs` still writes to `discoveryFeaturedSalons` (deprecated collection).
- Current status: Out of scope for this task; featured carousel may need a separate fix.
- Mitigation/next action: Track as part of NEW-DEBT-B B3 cleanup.

## Open Questions
1. Does the discovery feed now show cards on Android Expo Go? (Requires device verification — not done in this session.)

## Next Actions
1. Open Expo Go on Android, navigate to Home tab, confirm service cards appear (12 cards from 5 demo locations expected).
2. Run `CLEAR_SEED=true npm run seed:qa:dev` to verify QA seed also writes canonical docs correctly.
3. Continue Phase 3.5 release readiness work per `documentation/HANDOVER/07_ROADMAP_NEXT_STEPS.md`.
